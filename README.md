# Grayscale-Image-Reconstruction-NNMF-SGD-SVD
Given a set of grayscale images with missing parts, reconstruct the images by approximating missing parts using non-negative matrix factorization, stocastic gradient descent SGD and singular value decomposition SVD. From scratch
